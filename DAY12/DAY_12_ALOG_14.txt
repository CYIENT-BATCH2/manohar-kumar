Initialize 3 arrays as array1,array2,array3.
sizeof array/2 for 3 arrays
let us assume array1 is i,array2 is j,array3 is k.
if i < j then increment i.
if j < k then increment j.
if i=j=k,then print that equal value otherwise it will check next index.
if found one value then again checked till reach end to find the another common value 
if the value found it will print otherwise it print already founded value.
if common values are found it prints that values otherwise it will print no common values found.

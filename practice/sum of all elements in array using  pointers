#include <stdio.h>
int main()
{
    int a[5]={10,20,30,40,50};
    int i,sum=0;
    int*p;
    p=&sum;
    for(i=0;i<5;i++)
    {
        sum=sum+a[i];
    }
    printf("%d\n",*p);
      printf("%p\n",p);
}
